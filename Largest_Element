class Solution {
public:
    int largestElement(vector<int>& nums) {
        int large=INT_MIN;
        for(int i=0;i<nums.size(); i++){
            if(nums[i] > large)
            {
                large=nums[i];
            }
        }
        return large;
    }
};
